<%= turbo_stream.replace "#{dom_id(Topic.new)}_form" do %>
  <%= render 'form', topic: Topic.new %>
<% end %>
<%= turbo_stream.append "#{dom_id(@project)}_list" do %>
  <%= render @topic %>
<% end %>
